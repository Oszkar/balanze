[package]
name = "atomic_file"
description = "Perms-preserving atomic file replace with Unix crash durability and a documented Windows last-write caveat. Shared by the snapshot, statusline, settings, and OpenAI-gate writers."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
publish.workspace = true

[dependencies]

[dev-dependencies]
tempfile = "3"
